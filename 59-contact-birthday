public Boolean isBirthday(Contact c) {

Date todayDate = System.today();

if(todayDate.day() == c.Birthday.day() && todayDate.month() == c.Birthday.month()){
    return true;
}
else {
    return false;
}

}